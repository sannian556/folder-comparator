# 更新日志

> 程序里也能看到版本号：窗口右下角「设置」→ “关于”。报问题时先带上版本号，能省一轮排查。

## v1.1 — 2026-09-22

### 新增：深色模式

- 窗口右下角新增 **「设置」** 按钮（在「联系作者」旁边），里面的「外观」有三档：
  - **跟随系统** —— 跟着 Windows 的“应用模式”走，系统换主题时程序**实时**跟着变；
  - **浅色** —— 一直是浅色（默认）；
  - **深色**。
- 如果这台电脑的 Windows 没有“深色模式”这个设置（Windows 10 1809 以前），
  「跟随系统」那一档会自动**变灰**并写明原因，另外两档照常可用。
- 选择会被记住，存在 `%APPDATA%\文件比较器\主题.txt`；删掉该文件即恢复默认。
  （同目录下的 `设置.txt` 存的是忽略规则，**两个文件分开存**，互不覆盖。）

### 界面细节

深色不是换个背景色那么简单 —— WinForms 的系统控件在深色下要么白底、要么边框发白，所以做了这些：

- **列表**（四个分页里的文件列表）：改为自绘，行、表头、选中态、交替色全按主题上色；滚动条用 uxtheme 压深。
- **分页标签条**：换成自绘控件 `TabStrip`。原生 `TabControl` 的标签带边线和外框属于非客户区，`BackColor` 和 `Paint` 都改不动，深色下始终是几条白线。
- **进度条**：换成自绘控件 `ThemeProgress`。原生 `ProgressBar` 的轨道在深色下永远是浅色。
- **分组框**（“文件夹 A / B”）：边框是系统画的浅灰线，改为自己描一层主题色的框。
- **按钮**：深色下改成扁平样式；**禁用**状态的按钮系统会用浅色系绘制（深底上几乎看不见），改为自己按主题补画。
- 标签（Label）、复选框这类控件统一设为透明背景，避免留下一块块白底。

### 其它

- 界面线程里的未处理异常现在会写进程序目录的 `crash.log`，排错时不用再猜。
- 程序版本号由 1.0 升到 **1.1**（`AssemblyVersion` / `AssemblyFileVersion` = 1.1.0.0）。

### 已知限制

- 系统画的**对话框**（打开文件夹、消息框）始终跟随系统主题，不受程序内外观设置影响。
- 系统标题栏在 Windows 10 1809 以前无法改色。

## v1.0 — 2026-09-15

- 首个发布版：选择两个文件夹对比，结果分成「内容不同 / 仅在 B / 仅在 A / 完全相同」四类；
  双击文本文件可逐行查看差异；支持忽略规则、历史记录、把差异导出成 ZIP。
- 两套构建：`文件比较器(XP-Win7版).exe`（.NET 2.0，XP SP3 起）与
  `文件比较器(Win8-11版).exe`（.NET 4.0，界面用的 WebView2 网页版）。
